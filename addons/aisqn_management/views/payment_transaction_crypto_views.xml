<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- List View for Payment Transaction Crypto -->
    <record id="view_payment_transaction_crypto_list" model="ir.ui.view">
        <field name="name">payment.transaction.crypto.list</field>
        <field name="model">uni.payment.transaction.crypto</field>
        <field name="arch" type="xml">
            <list>
                <field name="user_id"/>
                <field name="currency_id"/>
                <field name="network"/>
                <field name="from_address"/>
                <field name="to_address"/>
                <field name="tx"/>
                <field name="amount"/>
                <field name="fee"/>
                <field name="amount_with_fee"/>
                <field name="status"/>
                <field name="type"/>
                <field name="requested_at"/>
                <field name="confirmed_at"/>
                <field name="is_verified_by_admin"/>
            </list>
        </field>
    </record>

    <!-- Form View for Payment Transaction Crypto -->
    <record id="view_payment_transaction_crypto_form" model="ir.ui.view">
        <field name="name">payment.transaction.crypto.form</field>
        <field name="model">uni.payment.transaction.crypto</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="user_id"/>
                        <field name="currency_id"/>
                        <field name="network"/>
                        <field name="from_address"/>
                        <field name="to_address"/>
                        <field name="tx"/>
                        <field name="amount"/>
                        <field name="fee"/>
                        <field name="amount_with_fee"/>
                        <field name="status"/>
                        <field name="type"/>
                        <field name="requested_at"/>
                        <field name="confirmed_at"/>
                        <field name="desc"/>
                        <field name="is_verified_by_admin"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action for Payment Transaction Crypto -->
    <record id="action_payment_transaction_crypto" model="ir.actions.act_window">
        <field name="name">Payment Transaction Crypto</field>
        <field name="res_model">uni.payment.transaction.crypto</field>
        <field name="view_mode">list,form</field>
    </record>

  
  
</odoo>
